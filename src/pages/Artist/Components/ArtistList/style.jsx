import styled from "styled-components";

export const ListArtist = styled.div`
  .show-more {
    display:inline-block;
    font-weight: 700;
    margin-left: 1.5rem;
    margin-top:1rem;
    cursor: pointer;
  }
`;
